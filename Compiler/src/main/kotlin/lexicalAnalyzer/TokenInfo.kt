package lexicalAnalyzer

class TokenInfo(token:String, tokenType:String) {
    var token:String = "NULL"
    var tokenType = "NULL"
    init {
        this.token = token
        this.tokenType = tokenType
    }
}